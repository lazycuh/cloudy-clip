import { describe, expect, it } from 'vitest';

import { KeyboardShortcutComponent } from './keyboard-shortcut.component';

describe(KeyboardShortcutComponent.name, () => {
  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
