import { describe, expect, it } from 'vitest';

import { ClipboardHistoryComponent } from './clipboard-history.component';

describe(ClipboardHistoryComponent.name, () => {
  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
